name: WL local
on: [push,workflow_dispatch ]
jobs:
   buildJob:
     runs-on: ubuntu-latest
     steps:
      - name: buildJob
        run: echo "Build en cours"
   testJob:
     runs-on: ubuntu-latest
     steps:        
      - name: testJob
        run: echo "test en cours"
      
